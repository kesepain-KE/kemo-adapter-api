name: Version and update safety

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: version-update-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  version-and-update:
    name: Version and update (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Validate release metadata
        run: python -m tests --suite version -q

      - name: Validate the Kemo 1.0 shared wire contract
        run: python -m tests.contracts.kemo_v1 -q

      - name: Run update unit safety tests
        run: python -m tests --suite update-unit -q

      - name: Run temporary Git repository integration tests
        run: python -m tests --suite update-git -q

      - name: Run Provider execution safety tests
        run: >-
          python -m tests --suite protocol --suite providers --suite storage --suite architecture -q

      - name: Check offline Provider templates and guided examples
        run: >-
          python -m tests --suite templates -q

      - name: Check the real remote without modifying source
        run: python update.py --check

      - name: Verify UTF-8 Git boundary and source syntax
        run: |
          python -m compileall -q update.py update core api providers web template tests
          git diff --check
